"""
项目级共享常量配置。

集中维护数据窗口、业务成本口径与训练入口默认参数，
避免不同模块重复定义同一份配置。
"""

# ==============================================================================
# 数据相关常量
# ==============================================================================
# 历史输入窗口长度，表示用过去多少天的数据预测下一天需求。
HISTORY_WINDOW_DAYS = 7
# 年持有成本折算为周成本时使用。
WEEKS_PER_YEAR = 52.0
# 缺失售价时使用的兜底价格。
DEFAULT_FALLBACK_SELL_PRICE = 5.0

# ==============================================================================
# 业务成本相关常量
# ==============================================================================
# 售价到进货成本的利润率换算系数。
PROFIT_MARGIN_RATE = 0.35
# 年化库存持有成本率。
ANNUAL_HOLDING_COST_RATE = 0.20
# 每次下单都会产生的固定成本。
FIXED_ORDERING_COST = 5.0
# 默认缺货惩罚系数，可被命令行参数覆盖。
DEFAULT_PENALTY_COEF = 10.0
# 保留该别名，兼容旧代码中的命名习惯。
REPUTATION_PENALTY_MULTIPLIER = DEFAULT_PENALTY_COEF

# ==============================================================================
# 训练入口默认参数
# ==============================================================================
# 训练 DataLoader 默认 batch size。
DEFAULT_BATCH_SIZE = 128
# 默认训练轮数。
DEFAULT_EPOCHS = 5
# wandb/project 默认实验名。
DEFAULT_EXP_NAME = "8008"
# 预测模型优化器默认学习率。
DEFAULT_LEARNING_RATE = 1e-5
# 总损失的默认组合策略。
DEFAULT_LOSS_STRATEGY = "balanced_sum"
# 预测损失在总损失中的默认权重。
DEFAULT_LOSS_ALPHA = 0.5
# 梯度裁剪默认阈值。
DEFAULT_GRAD_CLIP_NORM = 1.0
# ABCA 求解器默认最大迭代次数。
DEFAULT_SOLVER_MAX_ITER = 50
# ABCA 求解器默认种群规模。
DEFAULT_SOLVER_POP_SIZE = 30
